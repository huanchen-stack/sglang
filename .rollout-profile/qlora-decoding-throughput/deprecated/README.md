# Deprecated Raw Runs

This folder keeps old raw serving sweeps that should not be used for the
current figures.

Current deliverables use:

```text
.rollout-profile/qlora-decoding-throughput/measurements_14b_current/
```

The archived runs include earlier 32B attempts, retry-only CSGMV data, and the
first 14B corrected pass that was superseded by the current all-GPU 14B sweep.
